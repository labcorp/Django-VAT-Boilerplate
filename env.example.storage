# Exemplos de variáveis de ambiente para configuração de storage
# Copie o conteúdo para o seu .env (ou ajuste no seu gerenciador de secrets) e preencha os valores.

# Opções de seleção de provedor:
# - STORAGE_PROVIDER: 'spaces' ou 's3' (override explícito; case-insensitive)
# - OU usar flags booleans: USE_SPACES=true ou USE_S3=true

# Exemplo: usar DigitalOcean Spaces (recomendado para DO)
# STORAGE_PROVIDER=spaces
# USE_SPACES=true

# Credenciais e configurações para DigitalOcean Spaces
# SPACES_ENDPOINT: exemplo https://nyc3.digitaloceanspaces.com
# SPACES_REGION: exemplo nyc3
# SPACES_BUCKET: nome-do-bucket
# SPACES_KEY: chave de acesso
# SPACES_SECRET: segredo
# SPACES_STATIC_PATH: caminho dentro do bucket para arquivos estáticos (default: static)
# SPACES_MEDIA_PATH: caminho dentro do bucket para uploads (default: media)
# SPACES_STATIC_DOMAIN: (opcional) domínio personalizado para estáticos, p.ex. cdn.exemplo.com
# SPACES_MEDIA_DOMAIN: (opcional) domínio personalizado para media; se vazio usa STATIC_DOMAIN

# Exemplo (descomente e preencha):
# SPACES_ENDPOINT=https://nyc3.digitaloceanspaces.com
# SPACES_REGION=nyc3
# SPACES_BUCKET=meu-bucket
# SPACES_KEY=AKIA...DO...
# SPACES_SECRET=seu-segredo-aqui
# SPACES_STATIC_PATH=static
# SPACES_MEDIA_PATH=media
# SPACES_STATIC_DOMAIN=cdn.exemplo.com
# SPACES_MEDIA_DOMAIN=media.exemplo.com

# -----------------------------
# Exemplo: usar AWS S3
# STORAGE_PROVIDER=s3
# USE_S3=true

# Credenciais e configurações para AWS S3
# AWS_ACCESS_KEY_ID: chave
# AWS_SECRET_ACCESS_KEY: segredo
# AWS_STORAGE_BUCKET: nome-do-bucket
# AWS_S3_REGION: região AWS, p.ex. us-east-1
# AWS_S3_ENDPOINT_URL: (opcional) endpoint customizado para compatíveis S3
# AWS_STATIC_DOMAIN / AWS_MEDIA_DOMAIN: domínios customizados para recursos estáticos/media
# STATIC_PATH / MEDIA_PATH: alternativas para nomes de pastas se quiser sobrescrever defaults

# Exemplo (descomente e preencha):
# AWS_ACCESS_KEY_ID=AKIA...AWS...
# AWS_SECRET_ACCESS_KEY=seu-segredo-aws
# AWS_STORAGE_BUCKET=meu-bucket-aws
# AWS_S3_REGION=us-east-1
# # Se estiver usando um endpoint S3-compat (minio, etc.):
# # AWS_S3_ENDPOINT_URL=https://s3-minio.exemplo.com
# # Domínios customizados (opcional):
# # AWS_STATIC_DOMAIN=cdn.exemplo.com
# # AWS_MEDIA_DOMAIN=media.exemplo.com
# # Caminhos dentro do bucket (opcional):
# # STATIC_PATH=static
# # MEDIA_PATH=media

# -----------------------------
# Recomendações:
# - Prefira usar STORAGE_PROVIDER para deixar explícito qual provedor usar.
# - Não comite chaves reais; mantenha esse arquivo apenas como exemplo (env.example.storage).
# - Para ambientes de produção, use um gerenciador de segredo seguro ou variáveis no CI/CD.

# Nota: o projeto também respeita as variáveis STATIC_DOMAIN / MEDIA_DOMAIN como aliases.

# Se desejar servir arquivos estáticos (CSS/JS) também a partir do storage remoto (S3/Spaces),
# ative a flag abaixo. Por padrão, os static files continuam locais.
# USE_REMOTE_STATIC=false

# Se quiser que os objetos enviados ao bucket sejam legíveis publicamente (ACL 'public-read'),
# ative a das flags abaixo. Em produção é mais seguro aplicar uma policy no bucket/endpoint
# em vez de usar ACLs — use isso apenas se souber o que está fazendo.
# STORAGE_PUBLIC=false
# SPACES_PUBLIC=false  # alias para DigitalOcean Spaces
# AWS_PUBLIC=false     # alias para AWS S3
